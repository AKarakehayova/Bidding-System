<h2> Items: </h2>
<%= link_to 'New item', new_item_path %> 
<table>
  <% @items.each do |item| %>
  <tr>
    <td> <%= item.title %> </td>
   
    <td> <%= link_to 'More information', item_path(item)%></td>
 </tr>
 <% end %>
 </table>


